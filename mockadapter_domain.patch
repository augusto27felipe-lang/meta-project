diff --git a/backend/core/adapters/mock_adapter.py b/backend/core/adapters/mock_adapter.py
index 0000000..1111111 100644
--- a/backend/core/adapters/mock_adapter.py
+++ b/backend/core/adapters/mock_adapter.py
@@
-    def search(self, keyword: str, country: str = "us") -> List[Dict]:
-        if not self._running:
-            raise RuntimeError("Adapter not started")
-        # simulate network + scraping
-        time.sleep(0.1)
-        return [
-            {
-                "unique_id": f"{keyword}:{country}:1",
-                "keyword": keyword,
-                "country": country,
-                "title": f"Mock ad for {keyword}",
-            }
-        ]
+    def search(self, keyword: str, country: str = "us") -> List[Dict]:
+        if not self._running:
+            raise RuntimeError("Adapter not started")
+        # simulate network + scraping
+        time.sleep(0.1)
+        return [
+            {
+                "unique_id": f"{keyword}:{country}:1",
+                "keyword": keyword,
+                "country": country,
+                "title": f"Mock ad for {keyword}",
+                # gerar domínio fictício baseado na keyword
+                "domain": f"{keyword.lower()}.example.com",
+            }
+        ]
